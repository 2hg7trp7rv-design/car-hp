# 再レビュー・マージ前検証 — 2026-09-11

対象: PR #14（`fix/cbj-reliability-performance` → `v1`）。
本番ドメインはVercelの `v1` ブランチのデプロイに接続されていることをAPIで確認。

## 方針の再評価と追加修正

Guide / Column の原稿を共通テンプレートで全文表示する方針を維持。元JSONと正規化後の章・ブロック構成、および生成HTMLの本文・表・FAQ・出典を照合した。

- モバイルで非表示だった目次を、標準のdetails要素で開閉できる目次に変更。
- 記事専用のパンくずがあるページから、存在しない固定ナビ用の上部余白を除去。
- FAQの競合していた22個の表示ルールを整理。日本語の長い質問が見切れる問題を修正。
- 見出し・表の章番号除去で「10万円」「2.0L」「280馬力」など実値を削らないように変更。
- 原稿正規化のブロック欠落と数値の保持に回帰テストを追加。表491項目も生成HTMLと照合。

## 検証結果

- Node.js 22.23.2 / npm 10.9.4で `npm run check` 成功。ESLint、13テスト、型検査、本番ビルド、生成HTML照合、HTTP検査が通過。
- 7記事・64章・160段落全文・491表セル・41 FAQ・86出典を照合。194静的HTMLの内部リンクを検査。
- `npm audit --json`: 開発用を含め既知の脆弱性0件（この時点の結果）。
- ローカル本番ビルドの実ブラウザー確認: 390pxのスマートフォン幅と1280pxのPC幅。ホーム、ADASガイド、コラム、検索を確認。
- モバイル目次の開閉・章への移動、FAQ回答の開閉と質問全文の折り返しを確認。FAQテキストのscrollWidthとclientWidthが一致し、横方向の見切れなし。
- 検索で不一致語は0件、ADASをガイドに絞ると該当ガイド1件。Cookie拒否、設定の再表示、許可後の再読込で選択が保持されることを確認。
- PC記事のタイトルに横方向のはみ出しなし。全章の目次リンクと読み込み済み画像に欠落なし。

9月10日の記録にある「ブラウザー未確認」は今回解消。Core Web Vitalsの実利用計測および原稿の事実確認は別途必要。

## 配信連携の確認

Vercelの既存プレビューは成功。GitHubには別途Cloudflare Pages / Workersの失敗通知も存在するが、本番ドメインの配信元はVercelである。今回Cloudflare側の設定は変更していない。

マージ後は新しいコミットのVercel本番デプロイがREADYになり、本番ドメインへ割り当てられたことと、公開URLのHTTP・実画面を確認する。
