import type { ReactNode } from "react";
import { ReferenceFrame } from "@/components/learning/ReferenceFrame";
export default function LearningLayout({children}:{children:ReactNode}){return <ReferenceFrame>{children}</ReferenceFrame>;}
