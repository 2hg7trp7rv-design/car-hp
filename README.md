CBJ Atmospheric Reset (demo)

目的
- 添付の参考画像の「余白・ヘアライン・コラージュ・四隅ナビ」を、そのまま動くテンプレとして実装するための最小構成。

動かし方
- Vercelにimportして build / deploy (Node 20.x)
- ローカルなら `npm i` -> `npm run dev`

注意
- public/demo の画像はプレースホルダです。差し替えてください。
