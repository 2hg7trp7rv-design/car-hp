# 2026-09-12 再監査の修正

対象: `v1` の `a97103e0301d7f6d338d9e88581271240e3a99c3` に対する修正。

## 修正内容

- 公開判定を `lib/content/publication.ts` に統一。公開ステータスでも `publicState: draft / redirect` は配信しない。Guide / Columnの詳細取得が公開一覧を迂回する経路も削除。Car / Heritage、関連記事、サイト内検索にも適用する。`noindex` は公開のまま検索エンジン掲載を停止する。
- 本文画像の固定 `1600 × 1000` を廃止。ビルド時に303枚のローカル画像の寸法を取得し、HTMLに実寸を指定する。本文44枚は実ファイルと寸法を照合し、生成HTMLの属性値も検査する。
- Guide / Columnの構造化本文、表、FAQを検索対象へ追加。タイトル・概要より本文の重みを下げ、関係のない新着記事を返さない。検索用本文はAPI・初期表示データへ送らず、候補一覧の生成を共通化する。
- 未使用のUI・補助コード29ファイルと旧トップ画面のCSSを削除。空のフォント設定と何も処理しないクライアントラッパーも廃止。新たな未使用ソースは `verify:structure` で検出する。
- 共通CSSを車種・系譜・一覧の責務に分割し、使われない規則と上書き済み宣言を削除。記事の主要見出し・図版は基本定義を集約。目次の現在位置表示は `aria-current` を使う。

## 確認結果

- 19件の回帰テストが成功。4種類の内容に公開・noindex・下書き・アーカイブ・転送用データを混ぜ、実際の一覧・詳細・関連記事・検索で確認した。
- ESLint、型検査、本番ビルド、HTTP検査に成功。依存関係監査は既知の脆弱性0件。
- 全7記事・64章・160段落全文・491表セル・41 FAQ・86出典を生成HTMLと照合。194ページの内部リンクも検査した。
- 本番とローカルの本番ビルドを同じ画面幅390px / 1280px、高さ900pxで比較。トップ、ガイド一覧、ADASガイド、現代車カスタムコラム、DB7車種、F40系譜の合計5,988要素で、幅・文字・余白・色・配置など17項目の差異なし。
- ADASガイドの縦長図版を幅390pxで確認。読み込み前の表示は348 × 434.84375px、読み込み後は348 × 435px。従来の約217.5pxの高さ変化を0.15625pxに抑えた。
- 「エンブレム」でADASガイド1件、「バンプラバー」でADASと足回りガイド2件を表示。入力切替も確認し、ブラウザーエラーなし。
- 対象CSS合計は122,677 → 89,066バイト（約27%削減）。共通ファイルは3,701 → 459行。App Routerから辿れるソースは145ファイル、未使用ソース0件。

上記は対象ページと回帰条件の検証であり、全端末・全操作の網羅や実利用者のCore Web Vitals測定を意味しない。
本番反映はGitHub ActionsとVercelの結果を確認したうえで行う。
