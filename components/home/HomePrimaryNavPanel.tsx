// components/home/HomePrimaryNavPanel.tsx
"use client";

/**
 * 以前の HOME 上部ナビ（NEWS / CARS / COLUMN / GUIDE）用コンポーネント。
 * 現在は使わないので、既存の import を壊さないためのダミー実装にしておく。
 */
export default function HomePrimaryNavPanel() {
  // 何も描画しない = 上の大きなカードは出てこない
  return null;
}
