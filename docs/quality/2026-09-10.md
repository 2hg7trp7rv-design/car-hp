# 実装・検証記録 — 2026-09-10

対象: `2hg7trp7rv-design/car-hp`、ベース `7d6e7fe806eacad0e19ed7f09c837164bf6ef043`。
作業ブランチ: `fix/cbj-reliability-performance`。

## 修正

| 問題 | 変更 |
| --- | --- |
| ガイドで5章以降・長文・FAQ・出典が消える | Guide / Column を原稿全体を扱う共通テンプレートに統一 |
| 一致しない検索でも結果が並ぶ | 文字一致を必須にし、更新日等の加点は順位付けだけに使用 |
| 検索入力変更で古い結果が競合 | 前のリクエストを中止し、初期表示の種類も照合 |
| 依存関係の既知の脆弱性 | Next.js 16.3.4と関連・推移的依存を更新。全依存監査0件 |
| 全HTTPSホストの画像最適化を許可 | 現在使用するローカル画像に限定 |
| トップの全日本語フォントを先読み | 文字集合から再生成できるサブセットを導入 |
| 記事画像を一括先読み | メイン画像1枚を優先し、本文と関連記事は遅延読込 |
| 未公開レッスンの件数とリンク | 公開原稿から件数とリンクを算出 |
| Cookie設定を再表示できない | 全ページのフッターに操作ボタンを追加 |
| 無効な登録フォーム・共有リンク | 動かない部品を整理。著者リンクは通常のリンクで実装 |
| 削除済み記事への車種ページのリンク | 存在する関連記事だけを表示 |
| 原稿からのJSON-LD埋め込み | HTMLのscript終了文字をエスケープ。架空の著者・監修者の自動追加を撤去 |
| 検査が実際の画面と乖離 | 生成HTML照合と本番サーバーのHTTP検査を追加、PRで自動実行 |

古い記事固有テンプレート・不要なCSS・DOM全体の監視処理・追跡済みビルドキャッシュを整理。
READMEに構成、開発手順、記事追加、検査、フォント再生成の手順を記載。

## 実測と検証

- Node.js 22.23.2 / npm 10.9.4 / macOS arm64で `npm run check` 成功。
- ESLint警告0、回帰テスト10件成功、Next.js本番ビルド・型検査成功。
- 7記事、64章、160段落全文、41 FAQ、86出典を生成HTMLと照合。
- 194静的HTMLの内部リンクを検査。
- ローカル本番サーバーでページ・検索・404・画像最適化・外部画像拒否・Cookie設定リンク・サイトマップ・robotsを確認。
- トップの先読みフォント: 4,100,556 → 232,628 bytes（約94.3%削減、4書体合計）。
- ADASガイドのeager画像: 18 → 1枚。本文を増やした後も一括先読みを解消。
- `npm audit --json`: 開発用を含め既知の脆弱性0件。時点の結果であり恒久保証ではない。

## 残る確認

ブラウザーの管理ポリシー確認が利用できず、ブラウザー自動操作による実画面・スマートフォン表示の確認は未実施。
本番反映前にプレビューの見た目、目次、Cookie許可/拒否のクリック操作を確認する。
Lighthouse / Core Web Vitals の速度スコアは計測していない。

既存の原稿監査には編集品質の警告94件がある。出典・説明の充実や文章の正確さは別途編集レビューが必要。
本変更は公開記事の内容の事実確認や、本番へのマージ・公開を含まない。

依存・ワークフローの参照元:
[Next.js画像最適化のセキュリティ勧告](https://github.com/vercel/next.js/security/advisories/GHSA-2xp9-vwfh-vxw4)、
[actions/checkout](https://github.com/actions/checkout)、[actions/setup-node](https://github.com/actions/setup-node)。
