// lib/features.ts
// Feature toggles used by UI-level components.

/**
 * Temporarily disable car images across pages (cars list/detail/compare).
 * Re-enable by switching to `true`.
 */
export const ENABLE_CAR_IMAGES = false;
