# 学習記事の本文・表示検証（2026-09-25）

## 対象と変更

6テーマ・7コースの会話型教材を31回から41回へ拡張。エンジン、足回り、タイヤ／ブレーキ、運転支援、メンテナンスの各コースに2回ずつ追加し、既存の専門回も具体例から判断までつなげた。吸排気の既存16回は、量の定義・単位・成立条件を中心に修正した。既存URLと三段階の順序は維持している。

- 計算は前提・量の定義・単位・途中計算を明記し、架空例とメーカー公表値を区別した。
- 一次資料の該当箇所と数値は、テーマ別調査記録および独立レビュー記録に残した。
- グラフは数値横軸の間隔を反映し、縦軸範囲と0起点でない場合の注記、同じ値の表を併記する。
- 整備判断・購入判断を一律の結論にせず、車種・仕様・測定位置・運転条件で結論が変わる理由を説明する。

## 自動検証

Node.js 22.23.2で `npm run check` を実行し、終了コード0を確認した。

- ESLint警告0、ソース構造検証成功。
- 単体テスト31件成功。数値横軸、一定値系列、拡大した縦軸、本文スキーマ、フォント実体との整合を含む。
- Next.js本番ビルド・TypeScript検査成功。
- 41回・413会話の本文、図表、確認問題、出典、canonical、前後移動、サイトマップを生成HTMLと照合。
- 既存記事の代表8本も、70節・175段落・503表セル・46FAQ・92出典を生成HTMLと照合。
- 本番モードのローカルHTTP検査成功。学習48経路、フィルター、本文検索、不存在URLの404、画像最適化、Cookie設定、robots、サイトマップを確認。
- 禁止した数値レベル表記の検査は維持し、物理単位のあるグラフ縦軸範囲を誤検出しない回帰確認を追加。
- 既存コンテンツ全体の監査はerrors=0、warnings=94。今回の記事改善によって既存データすべての警告が解消されたとはしていない。

独立した出荷前レビューでも、現行7グラフの座標が有限かつ範囲内、7コース＋41回の事前生成経路に欠落・余剰がないことを確認した。4個のWOFF2をfontToolsで再読込し、cmap・SHA-256がmanifestと一致することを検証。合計227,496 bytes。

## ブラウザ確認

ローカル開発サーバーをCodex内ブラウザで操作した。

- 幅390px：本文16px、ページ全体の横はみ出しなし。ダンパーと停止距離のグラフ全体・凡例、表の内部横スクロールを目視確認。
- 確認問題をEnterキーで開き、解答本文とdetailsの開状態を確認。
- 次回リンクでダンパー記事からモーションレシオ記事へ移動。
- 「停止距離」を検索し、結果から該当記事へ移動。
- トップの6テーマと3段階、専門段階の絞り込み、出力比較グラフの298〜304 PSという拡大範囲の注記を確認。
- 幅1280px：DOM上の左右2列、グラフ幅、ページ横はみ出しがないことを確認。デスクトップ画面全体はホスト画面の撮影範囲制限があるため、全面の目視確認とは区別する。
- 対象ページで取得したブラウザconsoleのerror／warnは0件。

## 編集上の到達点と限界

専門知識を用いて条件や数値を読み解く教材として補強した。車両の実測、比較試験、取材、資格を持つ整備士による監修を実施したとはしていない。独自の測定データや専門家の事例が加わる余地は残る。

本記録の自動検証とブラウザ確認はローカルの結果。公開環境の反映は、別途GitHubのマージ結果、Vercelのデプロイ、公開URLの本文を照合する。
